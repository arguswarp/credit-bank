spring:
  datasource:
    url: ${DB_URL}
    username: ${DB_USERNAME}
    password: ${DB_PASSWORD}
  jpa:
    generate-ddl: false
    show-sql: false
  liquibase:
    enabled: true
deal:
  rest-client:
    root-uri: 'http://localhost:8080/calculator/'
logging:
  level:
    root: info