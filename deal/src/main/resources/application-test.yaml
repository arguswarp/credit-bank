spring:
  datasource:
    url: jdbc:tc:postgresql://localhost:5432/test
    username: admin
    password: admin
  jpa:
    generate-ddl: false
    show-sql: true
  liquibase:
    enabled: true
deal:
  rest-client:
    root-uri: 'http://localhost:8090/calculator/'
logging:
  level:
    root: info